{
  "name": "coc-quicktype",
  "version": "0.0.2",
  "description": "coc-quicktype is capable of generating strongly typed models or serializers from JSON queries, making it easy to process JSON in a type safe manner in many programming languages.",
  "author": "chuyanlong <a1008611abcd@126.com>",
  "license": "MIT",
  "main": "lib/index.js",
  "keywords": [
    "coc.nvim"
  ],
  "engines": {
    "coc": "^0.0.82"
  },
  "scripts": {
    "watch": "node esbuild.mjs --watch",
    "build": "node esbuild.mjs",
    "prepare": "node esbuild.mjs"
  },
  "devDependencies": {
    "coc.nvim": "^0.0.83-next.18",
    "esbuild": "^0.19.8",
    "typescript": "^5.3.3"
  },
  "activationEvents": [
    "onLanguage:python",
    "onLanguage:typescript",
    "onLanguage:java",
    "onLanguage:go",
    "onLanguage:swift",
    "onLanguage:dart",
    "onLanguage:cpp",
    "onLanguage:javascript",
    "onLanguage:php",
    "onLanguage:rust",
    "onLanguage:cs",
    "onLanguage:elm"
  ],
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "coc-quicktype configuration",
      "properties": {
        "coc-quicktype.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable coc-quicktype extension"
        }
      }
    },
    "commands": [
      {
        "command": "quicktype.generate",
        "title": "Convert the copied JSON from the clipboard to a strongly typed language"
      }
    ]
  },
  "dependencies": {
    "quicktype-core": "^23.2.6"
  }
}
